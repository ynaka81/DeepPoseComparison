machine:
  timezone:
    Asia/Tokyo
  python:
    version: 2.7.6

general:
  artifacts:
    - _build/html

dependencies:
  override:
    - pip install pyflakes pylint

test:
  pre:
    - find -name '*.py' | xargs pyflakes
    - find -name '*.py' | xargs pylint
  post:
    - make html
