machine:
  timezone:
    Asia/Tokyo
  python:
    version: 2.7

general:
  artifacts:
    - docs/_build/html

dependencies:
  pre:
    - pip install pyflakes pylint
    - pip install sphinx

test:
  pre:
    - pyflakes modules
    - pylint modules
  post:
    - cd docs && make html
